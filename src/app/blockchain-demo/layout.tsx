"use client";

import { ReactNode } from "react";
import { Web3Provider } from "@/providers/Web3Provider";

export default function BlockchainDemoLayout({
  children,
}: {
  children: ReactNode;
}) {
  return children;
}
